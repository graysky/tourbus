<% for column in Band.content_columns %>
<p>
  <b><%= column.human_name %>:</b> <%=h @band.send(column.name) %>
</p>
<% end %>

<%= link_to 'Edit', :action => 'edit', :id => @band %> |
<%= link_to 'Back', :action => 'list' %>
