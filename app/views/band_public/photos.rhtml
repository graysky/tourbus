<div id="main_no_side">

<h1><%= @band.name %> <span><%= public_band_link "Back To Main Band Page", @band %></span></h1>

	<%= render(
	:partial => "shared/photo_preview", 
	:locals => 
		{
		:photos => @band.photos,
		:type => Photo.Band,
		:id => @band.id,
		:full => true
		}) 
	%>
	
</div>
