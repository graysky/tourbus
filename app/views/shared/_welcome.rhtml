<%
  # Parameters:
  # logged_in => if a user is logged in
%>
<div class="welcome_msg">

<% if logged_in? 
  user = logged_in
%>

Help Make Tourbus better, <%= user.name %>!
<br/><%= link_to("Sign Up", :controller=> "signup")  %>

<% else %>

You should join New User!
<br/><%= link_to("Sign Up", :controller=> "signup")  %>

<% end %>

</div>

