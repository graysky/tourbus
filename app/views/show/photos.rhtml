<div id="main_no_side">

<% @header_title = @show.formatted_title + "/Photos" %>


<span><%= public_show_link "Back To Main Show Page", @show %></span>

	<%= render(
	:partial => "shared/photo_preview", 
	:locals => 
		{
		:photos => @show.photos,
		:type => Photo.Show,
		:id => @show.id,
		:full => true
		}) 
	%>
	
</div>
