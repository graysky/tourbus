<div id="main_no_side">

<% @header_title = @venue.name + "/Photos" %>

	<%= render(
	:partial => "shared/photo_preview", 
	:locals => 
		{
		:photos => @venue.photos,
		:type => Photo.Venue,
		:id => @venue.id,
		:full => true
		}) 
	%>
	
</div>
