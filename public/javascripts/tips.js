function addAttending(event)
{
  Tooltip.toggle($('add_attending_link'), event);
}

function removeAttending(event)
{
  Tooltip.toggle($('remove_attending_link'), event);
}

function addWatching(event)
{
  Tooltip.toggle($('add_watching_link'), event);
}

function removeWatching(event)
{
  Tooltip.toggle($('remove_watching_link'), event);
}

function addFave(event)
{
  Tooltip.toggle($('add_fave_link'), event);
}

function removeFave(event)
{
  Tooltip.toggle($('remove_fave_link'), event);
}