<% 
require_dependency "fixture_helper"
fix = FixtureHelper # Shorten typing
%>

<% 
  p "Creating #{fix.num_tags} tags..."
  
  1.upto( fix.num_tags ) do |num|
%>
tag_<%= num %>:
  id: <%= num %>
  name: <%= fix.get_tag(num-1) %>
<% end %>